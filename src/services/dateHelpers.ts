const getLongWeekday = (day: Date) => {
  return day.toLocaleString("en-US", { weekday: "long" });
};

const getLongMonth = (day: Date) => {
  return day.toLocaleString("en-US", { weekday: "long" });
};
